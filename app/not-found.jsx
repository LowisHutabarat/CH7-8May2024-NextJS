export default function NotFoundPage() {
    return (
    <main className="not-found">
        <h1>Not Found</h1>
        <p>Unfortunately, we could not find thr=e requested page or resource</p>
    </main>
    );
}